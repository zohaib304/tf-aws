# Terraform module for provisioning resources in AWS

This Terraform module allows you to provision various resources in AWS, including EC2 instances, S3 buckets, and RDS databases. It is designed to be flexible and reusable, making it easy to manage your infrastructure as code.

- **tf-aws-vpc-baseline** folders contains the baseline VPC configuration, including subnets, route tables.